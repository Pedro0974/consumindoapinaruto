import styled from 'styled-components';

export const VillagesCard = styled.div`
    display: flex;
    flex-wrap: wrap;
`;

export const VillageItem = styled.div`
    width: 20%; /* 20% para 5 colunas */
    box-sizing: border-box;
`;